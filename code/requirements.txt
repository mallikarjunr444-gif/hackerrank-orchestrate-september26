# Buy or Wait? — AI Financial Decision Agent
#
# The submitted evaluation path has no required third-party Python packages.
# It uses Python 3.9+ standard-library modules plus bundled OCR text in
# code/ocr_texts/ for deterministic offline receipt parsing.
#
# Optional live OCR fallback for local experimentation only:
#   pillow>=9.0.0
#   pytesseract>=0.3.10
#
# If those optional packages are installed, the machine must also have the
# Tesseract OCR binary available. They are intentionally commented out so
# `pip install -r requirements.txt` remains portable in the HackerRank runner.
